
import React from 'react';
import { ConfigInterface } from '../../utilities/fieldTypes';

interface SurveyIndexProps {
    fetching: boolean;
    config: ConfigInterface;
}

function SurveyIndex(props: SurveyIndexProps) {

}

export default SurveyIndex;
