import {fetchConfig} from './fetch-config';
import {postSubmission} from './post-submission';
import {postVerification} from './post-verification';

const backend = {
    fetchConfig,
    postSubmission,
    postVerification,
};

export default backend;
